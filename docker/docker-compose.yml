version: '3.9'
services:
  backend:
    build: ../backend
    ports:
      - "8000:8000"
    environment:
      - VIRUSTOTAL_KEY=${VIRUSTOTAL_KEY:-}
      - ABUSEIPDB_KEY=${ABUSEIPDB_KEY:-}
    restart: unless-stopped
